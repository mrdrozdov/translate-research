#!/usr/bin/env python

from setuptools import setup

setup(name='ProfessorTranslate',
      description='Research code for neural machine translation.',
      version='0.0.1',
      packages=['professor'])
